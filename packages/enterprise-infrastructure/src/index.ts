/**
 * Infrastructure adapters are introduced incrementally in Phase 3.
 *
 * This package may depend on Application contracts and Domain objects. Inner
 * layers must not depend on this package.
 */
export {};
